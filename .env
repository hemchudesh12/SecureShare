# SecureShare — environment configuration
# This file is loaded automatically on startup via python-dotenv.
# NEVER commit this file to version control (add .env to .gitignore).

# ── Gmail SMTP (App Password) ────────────────────────────────────────────────
MAIL_USERNAME=hemchudesha06@gmail.com
MAIL_PASSWORD=xspa qofm uabc xxad

# ── Flask secret key ─────────────────────────────────────────────────────────
# SECRET_KEY=change-this-in-production
